# Configuration file for the Speaker tool
# Copy this file to .env and fill in your API keys.
#
# ~/.local/share/speaker/.env

# Order of LLM models to use. The script will try to use models in this order.
# Available options: "gemini", "openai", "deepseek", "ollama"
LLM_FALLBACK_ORDER="gemini,openai,deepseek,ollama"

# --- API Configuration ---

# Google Gemini
# API key can be obtained from Google AI Studio: https://aistudio.google.com/app/apikey
GEMINI_API_KEY="Your_Gemini_API_Key"
# Optionally specify a comma-separated list of Gemini generation models in priority order.
# Example: GEMINI_MODELS="gemini-pro,gemini-2.1"
# If not set, a single model can be specified with GEMINI_MODEL or defaults to "gemini-pro".
GEMINI_MODELS="gemini-pro-latest,gemini-2.5-pro,gemini-2.5-flash,gemini-2.5-flash-lite"
GEMINI_MODEL="gemini-2.5-flash"

# OpenAI
# API key can be obtained from OpenAI dashboard: https://platform.openai.com/api-keys
OPENAI_API_KEY="Your_OpenAI_API_Key"
OPENAI_MODEL="gpt-4o-mini" # Can be changed to e.g., "gpt-4.1-nano"

# DeepSeek
# API key can be obtained from DeepSeek dashboard: https://platform.deepseek.com/
DEEPSEEK_API_KEY="Your_DeepSeek_API_Key"
# Available models: "deepseek-chat", "deepseek-coder"
DEEPSEEK_MODEL="deepseek-chat"

# Ollama (for locally run models)
# Provide the full URL to the Ollama server. Check out https://ollama.com/library for more info.
OLLAMA_BASE_URL="http://localhost:11434"
# The name of the model to use (e.g., "llama3.1:latest", "mistral:latest", "mistral-nemo:latest")
OLLAMA_MODEL="mistral-nemo:latest"

# --- TTS (Text-to-Speech) Configuration ---

# Order of TTS engines to use. Available options: "gemini", "gtts". Gtts is local and doesn't need api key.
TTS_FALLBACK_ORDER="gtts,gemini"

# Google TTS model name. By default, a standard, high-quality model will be used.
# You can experiment with names, e.g., "text-to-speech-1"
GEMINI_TTS_MODEL="gemini-2.5-flash-tts"

# Optional: target language code (e.g., "en", "pl") for translation when using the
# translate flag (-T/--translate). If not set, defaults to "en".
TRANSLATE_TO_LANG="en"

